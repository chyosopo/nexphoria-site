node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store
qa_shots/baseline/
.playwright-mcp/

# QA screenshot output — regenerate locally, never commit (~300MB)
qa_shots/
qa/
qa-screenshots*/
qa-rebuild-final/
.v0-out/
.vercel
.cf-deploy/

# Agent preview renders — regenerate on demand, never commit.
# Added 2026-08-14 after `git add -A` swept 25MB of them into four commits.
shots/

# Apex deploy staging copy — built by docs/APEX-DEPLOY.md, never committed.
.deploy-apex/
